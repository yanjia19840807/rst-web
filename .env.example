# Leave empty in local UI to use Vite /api proxy → localhost:8080.
# Set an absolute URL only when the SPA talks to API cross-origin.
VITE_API_BASE_URL=
VITE_ENABLE_MSW=true
VITE_AZURE_TENANT_ID=
VITE_AZURE_CLIENT_ID=
VITE_AZURE_API_SCOPE=
